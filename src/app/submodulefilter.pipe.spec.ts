import { SubmodulefilterPipe } from './submodulefilter.pipe';

describe('SubmodulefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SubmodulefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
